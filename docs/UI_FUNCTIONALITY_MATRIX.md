# UI Functionality Matrix

This document lists all user-interactive controls across the LEGO Command Center application, tracking their implementation state, server-side handlers, external integration side effects, and automated test coverages.

---

| Page | Control / Button | Component | Server Handler | Domain Service | External Side Effect | Expected State Change | Status | Test Name | Live Verification |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| `/sales` | **Sell Item** | `SellModal` | Server Action (implicit form action) | `InventoryService.recordSale` | None | Decrements company/personal stock balance, logs sale transaction ledger, inserts commercial sale and sale item invoice. | `REAL_VERIFIED` | `tests/inventory.test.ts` (concurrency and invariants check) | Verified locally |
| `/purchases` | **Log Purchase Intake** | `PurchaseForm` | Server Action (implicit form action) | `InventoryService.recordPurchase` | None | Increments inventory stock balance, recalculates variant weighted average cost basis, logs purchase item ledger. | `REAL_VERIFIED` | `tests/inventory.test.ts` (moving average cost intake verification) | Verified locally |
| `/inventory/[id]` | **Transfer Stock** | `TransferModal` | Server Action (implicit form action) | `InventoryService.transferStock` | None | Decrements source warehouse, increments destination warehouse, recalculates moving average cost on destination. | `REAL_VERIFIED` | `tests/inventory.test.ts` (internal stock transfers) | Verified locally |
| `/inventory/[id]` | **Manual Correction** | `AdjustModal` | Server Action (implicit form action) | `InventoryService.adjustStock` | None | Updates inventory balances with positive/negative quantity corrections, logs audit log entries. | `REAL_VERIFIED` | `tests/inventory.test.ts` (ledger integrity manual adjustments) | Verified locally |
| `/inventory/import` | **Onboard CSV / Excel** | `ImportWizard` | `commitImportAction` | `InventoryService.recordPurchase` / `import` | None | Spawns missing products/variants, creates starting balances, logs audit and `IMPORT` transaction logs. | `REAL_VERIFIED` | `tests/inventory.test.ts` (bulk import validation boundaries) | Verified locally |
| `/marketplaces` | **Connect / Test Connection** | `MarketplaceManager` | `testMarketplaceConnectionAction` | `ShopifyAdapter.testConnection` / `test` | Real Shopify API request in `REAL` mode | Performs credential authorization checks against Shopify's `GET /admin/api/2026-07/shop.json`. | `REAL_VERIFIED` | `tests/shopify.test.ts` (Shopify adapter connection verification) | Verified via `test:live:shopify` |
| `/marketplaces` | **Disconnect** | `MarketplaceManager` | `disconnectMarketplaceAction` | None | None | Resets database marketplace connected state to disconnected, clears configurations. | `REAL_VERIFIED` | `tests/shopify.test.ts` (connection settings management) | Verified locally |
| `/marketplaces/shopify/import` | **Import & Match Listings** | `ShopifyImportWizard` | `commitShopifyImportAction` | `InventoryService` / onboarding engine | None | Persists missing catalog items, initializes company inventory balance, logs `IMPORT` ledger transactions. | `REAL_VERIFIED` | `tests/shopify.test.ts` (catalog matching resolution imports) | Verified locally |
| `/api/webhooks/shopify` | **Shopify Webhook POST** | None (Route Handler) | `POST` Webhook Ingestion | `MarketplaceEventProcessor` | None | Validates Shopify headers, computes raw timing-safe HMAC signature, persists event, decrements stock atomically under row-level locks, schedules outbound sync. | `REAL_VERIFIED` | `tests/shopify.test.ts` (HMAC signatures and idempotency runs) | Verified via `test:live:shopify` |
| `/alerts` | **Resolve Alert & Reprocess** | `AlertsManager` | `resolveUnmatchedSkuAction` | `MarketplaceEventProcessor` | None | Updates variant SKU (permanent) or edits raw event JSON (one-time), triggers event re-run, resolves alert. | `REAL_VERIFIED` | `tests/shopify.test.ts` (unknown SKU mismatch correction) | Verified locally |
| `/marketplaces` | **Retry Sync Job** | `MarketplaceManager` | `retrySyncJobAction` | `SyncService.processJob` | Real Shopify inventory or pricing push in `REAL` mode | Resets `SyncJob` to pending, triggers worker thread to process job, updates status. | `REAL_VERIFIED` | `tests/shopify.test.ts` (retry sync job execution boundaries) | Verified via `test:live:shopify` |
| `/marketplaces/shopify/reconciliation` | **Push Central to Shopify** | `ShopifyReconciliation` | `triggerOutboundSyncAction` | `SyncService` | Real Shopify inventory push in `REAL` mode | Creates `SyncJob` with operation `SYNC_INVENTORY`, triggers async inventory update. | `REAL_VERIFIED` | `tests/shopify.test.ts` (reconciliation push overrides) | Verified via `test:live:shopify` |
| `/marketplaces/shopify/reconciliation` | **Adjust Local Ledger** | `ShopifyReconciliation` | `resolveReconciliationCorrectionAction` | `InventoryService.adjustStock` | None | Creates a local `STOCK_COUNT_CORRECTION` ledger transaction to align inventory balances, triggers sync. | `REAL_VERIFIED` | `tests/shopify.test.ts` (reconciliation local ledger adjustments) | Verified locally |
| `/pricing` | **Refresh Market Prices** | `PricingManager` | `triggerPriceCollectorAction` | `PricingCollector` | Real Apify actor run in `REAL` mode | Creates `REFRESH_PRICE_OBSERVATIONS` sync job, triggers worker to run Catawiki Apify scraper, parses datasets, writes observations. | `REAL_VERIFIED` | `tests/shopify.test.ts` (pricing observation collection) | Verified via `test:live:catawiki` |
| `/pricing` | **Accept and Sync Price** | `PricingManager` | `acceptPriceRecommendationAction` | None | Real Shopify pricing push in `REAL` mode | Updates database `MarketplaceListing` prices, schedules `"SYNC_PRICING"` outbound job, creates audit log entry. | `REAL_VERIFIED` | `tests/shopify.test.ts` (price recommendations acceptance) | Verified via `test:live:shopify` |
| `/inventory` | **Theme / Status Filters** | Table controls | Route navigation queries | None | None | Re-queries matching product sets from database based on parameters. | `REAL_VERIFIED` | Checked via detail routing | Verified locally |
| `/inventory` | **Sorting Controls** | Table headers | Route navigation queries | None | None | Re-orders product variant database lists. | `REAL_VERIFIED` | Checked via detail routing | Verified locally |
| `/analytics` | **Date Ranges** | Date widgets | Page queries | None | None | Re-aggregates transaction financials within dates. | `REAL_VERIFIED` | Checked via dashboard analytics | Verified locally |
